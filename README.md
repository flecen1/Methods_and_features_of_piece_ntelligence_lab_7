# Лабораторна робота 7 — генетичний алгоритм (Python)

Самостійна **практична робота** з курсу M&SAI: генетичний алгоритм у Python, пошук екстремуму для одного з п’яти варіантів функцій, графіки, консольний запуск і GUI.

## Репозиторій на GitHub (обов’язково для студентів)

Усі матеріали для виконання (код і `requirements.txt`) потрібно брати **лише з офіційного репозиторію**, щоб версії збігалися.

**Посилання:** [https://github.com/flecen1/Methods_and_features_of_piece_ntelligence_lab_7](https://github.com/flecen1/Methods_and_features_of_piece_ntelligence_lab_7)

Клонування (PowerShell або Git Bash):

```powershell
cd $env:USERPROFILE\Desktop
git clone https://github.com/flecen1/Methods_and_features_of_piece_ntelligence_lab_7.git
cd Methods_and_features_of_piece_ntelligence_lab_7
```

Якщо Git не встановлено — на сторінці репозиторію натисніть **Code → Download ZIP**, розпакуйте архів і працюйте з отриманою папкою.

**Покроково (коротко):** 0) установка залежностей; 1–4) `python gui.py` — чотири кнопки за завданнями методички (найпростіше). Альтернатива: `main.py` у консолі. Текст методичних вказівок викладач надає окремо.

## Встановлення

Рекомендовано віртуальне середовище **у папці клонованого репозиторію**:

```powershell
cd $env:USERPROFILE\Desktop\Methods_and_features_of_piece_ntelligence_lab_7
python -m venv .venv
.\.venv\Scripts\Activate.ps1
pip install -r requirements.txt
```

Якщо в консолі Windows «кракозябри» замість українських літер у JSON, увімкніть UTF-8:

```powershell
$env:PYTHONUTF8=1
```

## Запуск з командного рядка

```powershell
python main.py --variant 1 --generations 150 --population 40 --seed 7
python main.py --variant 1 --plot --plot-file figures\variant1.png --no-show
python main.py --variant 3 --json
```

Параметри:

- `--variant` — номер 1…5 (рядок таблиці варіантів у `variants.py` / у методичних вказівках).
- `--population` — розмір популяції (**не менше 2**).
- `--generations` — кількість поколінь (**не менше 1**).
- `--seed` — відтворюваність випадкових чисел (якщо не вказано, кожен запуск дає інший результат — це нормально для ГА).
- `--plot` — побудувати графік функції (1D — крива, 2D — поверхня).
- `--plot-file` — зберегти графік у файл; `--no-show` — не відкривати вікно matplotlib.

## GUI (рекомендовано для студентів)

```powershell
python gui.py
```

Простий інтерфейс: зліва — варіант і параметри, **чотири кнопки** (завдання 1–4), поле з результатом; справа — графіки.

1. **Оптимізація** — числовий результат для звіту.
2. **Графік функції** — зберігає `figures\variantN.png`.
3. **Експеримент** — три запуски (seed 1, 7; для 42 — pop=80, gen=150).
4. **GUI для звіту** — графік + збіжність; скріншот вікна (завдання 4).

Консольний `main.py` — за бажанням, якщо потрібна команда у звіті.

## Структура коду

| Файл | Призначення |
|------|-------------|
| `variants.py` | Усі 5 варіантів: межі, режим min/max, формула `f`. |
| `genetic_algorithm.py` | Реалізація ГА (турнір, арифметичний кросовер, гаусова мутація, елітизм). |
| `plotting.py` | Побудова графіків для звіту. |
| `main.py` | CLI. |
| `gui.py` | Графічний інтерфейс. |

Якщо викладач задає іншу формулу для варіанту, змініть відповідну функцію в `variants.py` (і за потреби `latex_hint`) — решта проєкту залишиться валідною.

## Відповіді на контрольні питання (коротко)

1. **Особливості ГА:** пошук із популяції, використання лише значень цільової функції (без градієнтів), стохастичні правила відбору, ітеративне «еволюційне» покращення наближення.

2. **Генетичні оператори:** селекція (відбір батьків), схрещування (кросовер), мутація; часто також елітизм і стратегії заміни популяції.

3. **Критерії зупинки:** ліміт поколінь; ліміт часу; поріг якості; відсутність покращення протягом кількох поколінь; мала відносна зміна найкращого значення тощо. У цьому репозиторії використовується зупинка за кількістю поколінь.

4. **Схема класичного ГА:** ініціалізація популяції → оцінка пристосованості → перевірка критерію зупинки → селекція → застосування кросоверу та мутації → формування нового покоління → повторення.

5. **Спільне використання операторів:** кросовер комбінує успішні фрагменти рішень; мутація підтримує різноманітність і допомагає виходити з локальних екстремумів; селекція направляє пошук; їх баланс впливає на швидкість збіжності і ризик передчасної збіжності.

## Примітка щодо варіантів 2 і 4

Для раціональної функції з знаменниками `(x−3)` та `(x−1)` на відрізку `[−4; 8]` є полюси. У коді для стійкості додано **штраф** у малих околах `x=1` та `x=3`. У звіті варто згадати це або показати графік з виключеними околами полюсів.
